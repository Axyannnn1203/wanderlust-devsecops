apiVersion: v1
kind: Service
metadata:
  name: wanderlust-backend
  namespace: wanderlust
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    app: wanderlust-backend
