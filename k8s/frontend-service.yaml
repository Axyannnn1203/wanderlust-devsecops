apiVersion: v1
kind: Service
metadata:
  name: wanderlust-frontend
  namespace: wanderlust
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: wanderlust-frontend
